export class Task {
  x: string;
  y: string;
  name: string;

  constructor(x: string, y: string, name: string) {
    this.x = x;
    this.y = y;
    if (name === undefined || name === '') {
      this.name = '任务';
    } else {
      this.name = name;
    }
  }


}
